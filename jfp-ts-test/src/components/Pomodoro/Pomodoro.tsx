import type { ReactElement } from "react"
import type { PomodoroTimer } from "@/libs/data"

export function Pomodoro(timer: PomodoroTimer):ReactElement {
  return (
    <div>
      <h1>Pomodoro</h1>
    </div>
  )
}